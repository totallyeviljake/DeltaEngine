using DeltaEngine.Core;
using DeltaEngine.Datatypes;

namespace DeltaEngine.Rendering
{
	/*TODO
	public class LineEntity : Entity2D
	{
		public LineEntity(string name, Content content, Rectangle drawArea)
			: base(name, content, drawArea)
		{
			DrawArea = drawArea;
		}

		//protected override void Render(Renderer renderer, Time time)
		//{
		//	var StartPosition = new Point(DrawArea.Left, DrawArea.Top);
		//	var EndPosition = new Point((DrawArea.Left + DrawArea.Width), (DrawArea.Top + DrawArea.Height);
		//	renderer.DrawLine(StartPosition, EndPosition, Color);
		//}
	}
	 */
}
